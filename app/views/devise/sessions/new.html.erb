<div class="row">
   <div class="col-xs-3 col-xs-offset-2 col-md-6 col-md-offset-4">
        <h2 class="title">Вход</h2>      
        <h4 style="color:#ffffff">ЧТобы войти в систему введите свой логин и пароль</h4><br>
   </div>
</div>
<div class="col-md-10 col-md-offset-1 content">
      <div class="col-md-8 col-md-offset-2">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                  <div class="form-group">
                        <%= f.email_field :email, :placeholder => "Эл.адрес", autofocus: true, class:"form-control" %>
                  </div>

                  <div class="form-group">
                        <%= f.password_field :password, autocomplete: "off", :placeholder => "Пароль", autofocus: true, class:"form-control" %>
                  </div>

                  <% if devise_mapping.rememberable? -%>
                    <div class="form-group">
                      <%= f.check_box :remember_me %>
                      <%= f.label "Запомнить меня" %>
                    </div>
                  <% end -%>
                  
                   <div class="col-md-offset-4">
                      <%= f.submit "Войти в систему", class: "follow-button" %>
                    </div>
                  
             <% end %>

                <%= render "devise/shared/links" %>

      </div>
</div>                

