<div class="row">
  <div class="col-xs-3 col-xs-offset-2 col-md-6 col-md-offset-5">
      <h2 class="title"><h2>Повторить инструкции подтверждения</h2></h2>
      <h4 style="color:#3c3e50;">Если еще не пришло письмо, повторите снова</h4><br>    
  </div> 
  <div class="col-md-10 col-md-offset-1 content">
      <div class="col-md-8 col-md-offset-2">
			<%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
  				<%= devise_error_messages! %>

				<div class="form-group">
				   <%= f.email_field :email, :placeholder => "Эл.адрес", autofocus: true, value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), class:"form-control" %> 
				</div>

				<div class="col-md-offset-4">
				    <%= f.submit "Повторить инструкцию подтверждения", class: "submit-button" %>
				</div>
			<% end %>
			<%= render "devise/shared/links" %>
	   </div>
  </div>
</div>




